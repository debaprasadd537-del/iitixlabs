{
  "buildCommand": "cd artifacts/iitix-labs && pnpm install && pnpm run build --skipLibCheck",
  "outputDirectory": "artifacts/iitix-labs/dist",
  "installCommand": "echo 'skip'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
